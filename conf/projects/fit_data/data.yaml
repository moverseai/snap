# @package _global_

SUBSET: [6, 7] # null # [1, 2] # [0, 1, 2]
DATA_ROOT: ???

data:
  train:
    loader:
      shuffle: false
    iterator:
      datasets:
        smpl:
          path: ${DATA_ROOT}
          subset: ${SUBSET}
        multiview_video:
          path: ${DATA_ROOT}
          subset: ${SUBSET}
